# The 58 MB BEM leadfield. Regenerated by `python forward.py` in ~3 min;
# every downstream summary it feeds is committed, so nobody needs the blob
# unless they are changing the forward model itself.
forward_model/cache/leadfield.npz

# Python
__pycache__/
*.pyc
.venv/
venv/
.ruff_cache/
.pytest_cache/
.mypy_cache/

# OS / editor
.DS_Store
.idea/
.vscode/
